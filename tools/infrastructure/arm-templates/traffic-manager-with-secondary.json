{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "uniqueDnsName": {
      "type": "string",
      "metadata": {
        "description": "Relative DNS name for the traffic manager profile, resulting FQDN will be <uniqueDnsName>.trafficmanager.net, must be globally unique."
      }
    },
    "path": {
      "type": "string",
      "metadata": {
        "description": "The endpoint path to route request from traffic manager"
      }
    },
    "primaryEndpointName": {
      "type": "string",
      "defaultValue": "primary",
      "metadata": {
        "description": "The name of the primary endpoint"
      }
    },
    "primaryEndpointHostName": {
      "type": "string",
      "metadata": {
        "description": "The primary external endpoint to monitor"
      }
    },
    "primaryEndpointLocation": {
      "type": "string",
      "metadata": {
        "description": "The loction to send requests from to the primary endpoint"
      }
    },
    "secondaryEndpointName": {
      "type": "string",
      "defaultValue": "primary",
      "metadata": {
        "description": "The name of the secondary endpoint"
      }
    },
    "secondaryEndpointHostName": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "The secondary external endpoint to monitor"
      }
    },
    "secondaryEndpointLocation": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "The location to send requests from to the secondary endpoint"
      }
    }
  },
  "resources": [
    {
      "type": "Microsoft.Network/trafficmanagerprofiles",
      "apiVersion": "2018-08-01",
      "name": "[parameters('uniqueDnsName')]",
      "location": "global",
      "properties": {
        "profileStatus": "Enabled",
        "trafficRoutingMethod": "Performance",
        "dnsConfig": {
          "relativeName": "[parameters('uniqueDnsName')]",
          "ttl": 60
        },
        "monitorConfig": {
          "protocol": "HTTP",
          "port": 80,
          "path": "[parameters('path')]"
        },
        "endpoints": [
          {
            "type": "Microsoft.Network/TrafficManagerProfiles/ExternalEndpoints",
            "name": "[parameters('primaryEndpointName')]",
            "properties": {
              "target": "[parameters('primaryEndpointHostName')]",
              "endpointStatus": "Enabled",
              "endpointLocation": "[parameters('primaryEndpointLocation')]"
            }
          },
          {
            "type": "Microsoft.Network/TrafficManagerProfiles/ExternalEndpoints",
            "name": "[parameters('secondaryEndpointName')]",
            "properties": {
              "target": "[parameters('secondaryEndpointHostName')]",
              "endpointStatus": "Enabled",
              "endpointLocation": "[parameters('secondaryEndpointLocation')]"
            }
          }
        ]
      }
    }
  ]
}